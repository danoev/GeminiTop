# Commit-safe firmware reports

JSON files here contain hashes, validated container/image metadata, filesystem
superblock metadata, and optional read-only file/ELF inventories. Firmware ZIPs,
BINs, extracted filesystems, and copied executables must stay outside Git.
